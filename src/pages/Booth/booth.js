import { useNavigate } from "react-router-dom";

function Booth(){

    let navigate = useNavigate();

return(
    <div className="iphone-frame">
       부스 배치도
       {/* 컴포넌트 4개 만들어서 버튼 클릭 시 각각 컴포넌트 보이도록 */}
        </div>

)
}

export default Booth;